public class Casting_1 
{
	public static void main(String[] args) 
	{

		/* 
		프로그램은 데이터와 명령으로 구성되어 있음
		데이터를 처리하기 위해서는 변수가 필요하고, 
		데이터의 종류에 따라서 다양한 자료형을 사용
		그런데 어떠한 경우에 서로 다른 자료형이 혼재되어 
		사용되기도 함, 서로 다른 자료형 사이의 연산을 위해서는
		자료형을 일치 시키는 작업이 필요.
		이처럼 서로 다른 자료형 사이에서
		자료형이 변화되는 것을 형변환(Casting) 라고 합니다.
		
		확대 형변환(Widening) : 자동으로 일어남 = > 묵시적 형변환(Implicit) (자동)

		축소 형변환(Narrowing) : Casting 해야됨 => 명시적 형변환(Explicit) (강제)
		(데이터 손실가능성 있음)

		자동규칙

		byte = > char, short => int => long => float => double (실수형이 더 크기 때문에 long < float)

		.수식에서의 형 변환
		 : char, byte, short 는 int 로 변환되고, float 는 double로 변환됨
			2개의 피연산자중 하나가 long 이면 나머지도 long 이고 결과도 long임
		:  2개의 피연산자중 하나가 double이면 나머지도 double이고 결과도 double임
		: 수식의 모든 float는 double로 변화되어 연산 수행

		. 대입문에서의 형 변환

		: 오른쪽 값이 왼쪽의 자료형으로 변환되어 저장됨
		: 모든 문자는 정수로 변환되어 저장됨
		: 실수형이 정수형으로 변환될때 소수점이하는 절삭되어 저장
		
		*/

		byte ll;
		int ii = 365;
		double dd = 77777.512;
		float ff;

		System.out.println(" 축소 형 변환 결과 " );
		ll = (byte)ii;
		ff = (float)dd;
		System.out.println(ll);
	
		System.out.println("Lee jiyoon");
	}
}
